import { StyleSheet } from "react-native";

export default StyleSheet.create({

    homeRow: {
        flexDirection: "row",
        justifyContent: "flex-end",
        marginBottom: 20
    },

    homeAddButton: {
        flexDirection: "row",
        alignItems: "center"
    },

    homeAddButtonText: {
        fontWeight: "bold",
        paddingLeft: 5,
        fontSize: 16
    }

});